{
  "name": "gp-api",
  "private": true,
  "workspaces": [
    "api",
    "deploy"
  ],
  "engines": {
    "node": ">=22.11.0"
  },
  "scripts": {
    "start": "npm run start -w=api",
    "start:dev": "npm run start:dev -w=api",
    "build": "npm run build -w=api",
    "deploy:dev": "npm run deploy:dev -w=deploy",
    "migrate:deploy": "npm run migrate:deploy -w=api"
  }
}
