name: Postman Tests

on:
  push:
    branches:
      - main
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  postman-tests:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout repository
        uses: actions/checkout@v3

      # Install Postman CLI
      - name: Install Postman CLI
        run: |
          curl -o- "https://dl-cli.pstmn.io/install/linux64.sh" | sh

      # Login to Postman CLI using API key
      - name: Login to Postman CLI
        run: postman login --with-api-key ${{ secrets.POSTMAN_API_KEY }}

      # Create environment file for tests
      - name: Create Test Environment
        run: |
          # Create environment file with all variables
          echo '{
            "name": "CI Environment",
            "values": [
              {
                "key": "protocol",
                "value": "http",
                "type": "default",
                "enabled": true
              },
              {
                "key": "host",
                "value": "localhost",
                "type": "default",
                "enabled": true
              },
              {
                "key": "port",
                "value": ":3000",
                "type": "default",
                "enabled": true
              },
              {
                "key": "rootPath",
                "value": "/v1",
                "type": "default",
                "enabled": true
              },
              {
                "key": "rootUrl",
                "value": "{{protocol}}://{{host}}{{port}}{{rootPath}}",
                "type": "default",
                "enabled": true
              },
              {
                "key": "testAdminUserEmail",
                "value": "${{ secrets.ADMIN_EMAIL }}",
                "type": "default",
                "enabled": true
              },
              {
                "key": "testUserPassword",
                "value": "${{ secrets.ADMIN_PASSWORD }}",
                "type": "default",
                "enabled": true
              }
            ]
          }' > test_env.json
          
          # Create new environment and store its ID
          ENV_ID=$(postman environment create -f test_env.json | jq -r '.environment.id')
          echo "POSTMAN_TEST_ENV_ID=$ENV_ID" >> $GITHUB_ENV

      # Run Postman collections with debug output
      - name: Run Postman Collections
        run: |
          echo "Testing with email: ${{ secrets.ADMIN_EMAIL }}"
          
          # Run collections with verbose output
          postman collection run ${{ secrets.ADMIN_COLLECTION_ID }} \
            --environment $POSTMAN_TEST_ENV_ID \
            --verbose
